# docker-compose.yml
version: '3.8'

services:
  app:
    # This tells Docker Compose to build the multi-stage Dockerfile
    build: .
    container_name: my-sandbox-app
    # Optional: Mount a volume to inspect session data from your host machine
    volumes:
      - ./session_data:/tmp/wasm_sessions