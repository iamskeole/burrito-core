FROM jupyter/minimal-notebook:latest

USER root
RUN pip install --no-cache-dir sympy numpy pandas
USER $NB_UID

# This is the key: it writes the connection info to the shared volume
CMD ["sh", "-c", "python -m ipykernel_launcher -f /tmp/jupyter-runtime/connection.json"]